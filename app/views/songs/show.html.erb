<h1><%= link_to @song.title, song_path(@song) %></h1>
<% if @song.artist.present? %>
<h1><%= link_to @song.artist.name, artist_path(@song.artist) %></h1>
<% else %>
<h3><%= link_to "no artist", edit_song_path(@song) %></h3>
<% end %>
<%= link_to "All Songs", songs_path %>
